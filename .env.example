# Example environment variables for `conversational_ai`
# Copy this file to `.env` and edit values to match your environment.

# LLM / Ollama
OLLAMA_MODEL_NAME=llama3:latest
OLLAMA_BASE_URL=http://localhost:11434

# Speech-to-text (Whisper)
WHISPER_MODEL_SIZE=base.en

# Audio
AUDIO_SAMPLE_RATE=16000
AUDIO_CHANNELS=1
# Leave empty to auto-select: AUDIO_DEVICE_INDEX=

# Text-to-speech
TTS_RATE=180
TTS_VOLUME=1.0

# Conversation behavior
SYSTEM_PROMPT=You are a helpful and friendly conversational AI. Keep responses concise and natural.
USER_NAME=User
AI_NAME=Assistant

# Optional: logging / debug
LOG_LEVEL=INFO

# Notes:
# - For Windows, activate venv using `venv\Scripts\activate` before running the app.
# - Set OLLAMA_* variables only if you use Ollama locally. Remove or adapt if using a different LLM backend.